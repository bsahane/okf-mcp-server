apiVersion: v1
kind: Service
metadata:
  name: okf-mcp-server
  labels:
    app: okf-mcp-server
    component: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 5001
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: okf-mcp-server
    component: mcp-server
