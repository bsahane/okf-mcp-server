apiVersion: apps/v1
kind: Deployment
metadata:
  name: okf-mcp-server
  labels:
    app: okf-mcp-server
    component: mcp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: okf-mcp-server
      component: mcp-server
  template:
    metadata:
      labels:
        app: okf-mcp-server
        component: mcp-server
    spec:
      containers:
        - name: okf-mcp-server
          image: okf-mcp-server:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 5001
              name: http
              protocol: TCP
          env:
            - name: MCP_HOST
              value: "0.0.0.0"
            - name: MCP_PORT
              value: "5001"
            - name: OKF_DATA_DIR
              value: /app/data
            - name: PYTHON_LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: okf-mcp-server-config
                  key: PYTHON_LOG_LEVEL
            - name: MCP_TRANSPORT_PROTOCOL
              valueFrom:
                configMapKeyRef:
                  name: okf-mcp-server-config
                  key: MCP_TRANSPORT_PROTOCOL
            - name: CORS_ENABLED
              valueFrom:
                configMapKeyRef:
                  name: okf-mcp-server-config
                  key: CORS_ENABLED
            - name: ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: okf-mcp-server-config
                  key: ENVIRONMENT
            - name: ENABLE_AUTH
              valueFrom:
                configMapKeyRef:
                  name: okf-mcp-server-config
                  key: ENABLE_AUTH
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: okf-mcp-server-secrets
                  key: SESSION_SECRET
            - name: SSO_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: okf-mcp-server-secrets
                  key: SSO_CLIENT_ID
                  optional: true
            - name: SSO_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: okf-mcp-server-secrets
                  key: SSO_CLIENT_SECRET
                  optional: true
            - name: SSO_CALLBACK_URL
              valueFrom:
                configMapKeyRef:
                  name: okf-mcp-server-config
                  key: SSO_CALLBACK_URL
                  optional: true
            - name: SSO_AUTHORIZATION_URL
              valueFrom:
                configMapKeyRef:
                  name: okf-mcp-server-config
                  key: SSO_AUTHORIZATION_URL
                  optional: true
            - name: SSO_TOKEN_URL
              valueFrom:
                configMapKeyRef:
                  name: okf-mcp-server-config
                  key: SSO_TOKEN_URL
                  optional: true
            - name: SSO_INTROSPECTION_URL
              valueFrom:
                configMapKeyRef:
                  name: okf-mcp-server-config
                  key: SSO_INTROSPECTION_URL
                  optional: true
            - name: MCP_HOST_ENDPOINT
              valueFrom:
                configMapKeyRef:
                  name: okf-mcp-server-config
                  key: MCP_HOST_ENDPOINT
                  optional: true
          envFrom:
            - secretRef:
                name: okf-mcp-server-secrets
                optional: true
          livenessProbe:
            httpGet:
              path: /health
              port: 5001
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 5001
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          volumeMounts:
            # Published snapshots, written by `okf-ingest build`; served read-only.
            - name: knowledge
              mountPath: /app/data
              readOnly: true
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
      volumes:
        - name: knowledge
          persistentVolumeClaim:
            claimName: okf-mcp-server-knowledge
            readOnly: true
      restartPolicy: Always
